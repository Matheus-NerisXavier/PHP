<!-- Vamos criar um menu interativo, muito parecido com o do banco, mas para gerenciar uma lista de tarefas (To-Do List).

Você vai precisar de um array (ex: $tarefas = [];) para guardar as tarefas.

O menu deve ter as seguintes opções:

Adicionar Tarefa: Pede ao usuário o nome da tarefa e a adiciona no final do array.

Listar Tarefas: Mostra todas as tarefas na tela. (Use um foreach). Se não houver tarefas, avise o usuário.

Remover Tarefa: Pede ao usuário o número da tarefa (o índice do array) que ele quer remover. (Dica: pesquise pela função unset() do PHP).

Sair: Termina o programa. -->